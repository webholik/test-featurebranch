components:
  schemas:
    Pet:
      type: object
      description: A pet
      required:
        - name
      properties:
        id:
          type: integer
          format: int64
        name:
          type: string
        typeOf:
          type: string
          enum: [Cat, Dog, Bird, Reptile]
        tag:
          type: string
